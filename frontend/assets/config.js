export const config = {
  pagination: {
    defaultLimit: 10
  },

  charts: {
    exponentialThreshold: 10000
  },

  ui: {
    toastDuration: 5000,
    updateInterval: 1000
  },

  display: {
    maxIdLength: 50,
    maxTypeLength: 50,
    defaultLocale: "en-US"
  }
};

export default config;
